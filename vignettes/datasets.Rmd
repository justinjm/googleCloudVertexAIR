---
title: "Datasets"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{datasets}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Setup

```{r auth, warning=FALSE}
library(googleAuthR)
library(googleCloudVertexAIR)

options(googleAuthR.scopes.selected = "https://www.googleapis.com/auth/cloud-platform")

gar_auth_service(json_file = Sys.getenv("GAR_SERVICE_JSON"))
```

# Set global arguements

```{r gcva-global-arguements}
projectId <- Sys.getenv("GCVA_DEFAULT_PROJECT_ID")
gcva_region_set("us-central1")
gcva_project_set(projectId)
```

# List datasets

```{r}
datasets <- gcva_list_datasets()
datasets
```

# Create a dataset

```{r}
dataset <- gcva_create_dataset(displayName = "example_dataset03",
                               gcsSource = "gs://gcatr-dev/bank_marketing.csv")
dataset
```

# List datasets (again)

```{r}
datasets2 <- gcva_list_datasets()
datasets2
```

# Delete dataset

```{r}
# gcva_delete_result <- gcva_delete_dataset(displayName = "example_dataset01")
# gcva_delete_result
```

# List datasets

```{r}
gcva_list_datasets()
```

